# EEVDF Visualizer

Animated visualizer for the Earliest Eligible Virtual Deadline First scheduling algorithm.

## Setup

```sh
uv sync
```

## Usage

Edit the `config` in `main.py` to set clients, weights, and request lengths, then run:

```sh
uv run main.py
```